---
alwaysApply: true
---

1) After major changes always run:
- `make fmt`
- `make ruff`
- `make vulture` 

And if any issues rise, make sure you address them.

2) After running the above, try `make ci` and ensure everything works fine there.